# Optional override for the FastAPI backend API root.
# Local development defaults to http://localhost:8000/api/v1.
# Production defaults to https://wazifny-bac.onrender.com/api/v1.
# Set this in Vercel only when deploying against a different API.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
